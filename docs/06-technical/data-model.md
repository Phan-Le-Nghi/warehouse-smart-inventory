# ERD / Data Model — Bản phục vụ báo cáo

## Trạng thái

`TBD`: Chưa có ERD, schema, thuộc tính, quan hệ hoặc cardinality nào được phê duyệt.

## Chỉ có phạm vi domain đã xác nhận

SKU, Warehouse, Stock, Movement, Transfer, Alert và Audit là các khái niệm domain có liên quan.

Danh sách này không phải database schema. Data Model canonical trong tương lai phải được dẫn xuất từ định nghĩa, yêu cầu, quy tắc và User Story đã duyệt, sau đó được liên kết tại đây.
