# API Contract — Bản phục vụ báo cáo

## Trạng thái

`TBD`: Chưa có API style, endpoint, operation, message, payload, error model hoặc authentication contract nào được phê duyệt.

Phạm vi API phụ thuộc vào User Story và architecture đã duyệt. Contract canonical trong tương lai sẽ nằm trong `vault/06-technical/`; tài liệu này cung cấp bản phục vụ báo cáo.
